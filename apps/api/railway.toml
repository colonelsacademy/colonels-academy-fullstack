[build]
builder = "nixpacks"
buildCommand = "corepack disable && npm install -g pnpm@10.0.0 && pnpm install --frozen-lockfile && pnpm run build --filter=@colonels-academy/api"

[deploy]
startCommand = "node apps/api/dist/index.js"
