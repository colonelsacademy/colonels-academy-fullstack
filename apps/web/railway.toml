[build]
builder = "nixpacks"
nixpacksConfigPath = "nixpacks.toml"
watchPatterns = ["apps/web/**", "packages/**"]

[deploy]
startCommand = "cd /app && pnpm --filter @colonels-academy/database run db:push && npx tsx packages/database/prisma/seed.ts && pnpm --filter @colonels-academy/web run start"
